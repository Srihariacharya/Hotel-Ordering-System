{
    "buildCommand": "npm run vercel-build",
    "outputDirectory": "Frontend/dist",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}